#include <stdio.h>
#include <stdlib.h>

/*
public class StringReversal {
    public static void main(String[] args) {
        String str = "Hello";
        char[] strArray = str.toCharArray();
        for (int i = 0, j = strArray.length - 1; i < j; i++, j--) {
            char temp = strArray[i];
            strArray[i] = strArray[j];
            strArray[j] = temp;
        }
        System.out.println(new String(strArray));
    }
}
*/
    //Objective: Write a program to initialize an array of integers and find the sum and average of the elements.
int main(){
    //Make string 'hello'
    char str[] = "hello";

    //init char array strArray
    
    //for loop up to strArray length, if i is less than j, go on; subtract from j

        //init a char temp that is strarray[i]

        //strArray[i] should equal strArray[j]

        //strArray[j] should then be temp

    //After. print out a new string that is strarray
}